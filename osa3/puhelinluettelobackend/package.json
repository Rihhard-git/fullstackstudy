{
  "name": "puhelinluettelobackend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../puhelinluettelofrontend && npm run build && cp -r dist ../puhelinluettelobackend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Veskus",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
